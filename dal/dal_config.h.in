#define DAL_VERSION_MAJOR @DAL_VERSION_MAJOR@
#define DAL_VERSION_MINOR @DAL_VERSION_MINOR@
#define DAL_VERSION_RELEASE @DAL_VERSION_RELEASE@
#define DAL_VERSION_STRING "@DAL_VERSION_STRING@"

/*
 * Define if HDF5 should be silenced by default when an
 * error occurs. When HDF5 is silenced, DAL will still
 * throw exceptions, but HDF5 will not print the stack
 * trace as well.
 */
#define QUIET_HDF5_ERRORS "@QUIET_HDF5_ERRORS@"

